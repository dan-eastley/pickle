{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "urn:pickle:schemas:decisions",
    "title": "Decisions Index",
    "description": "Authoritative list of decision IDs for a client version. Per-decision content lives in architectures/<client>/<version>/decisions/<decision-id>.json.",
    "type": "object",
    "required": ["decisions"],
    "properties": {
        "decisions": {
            "type": "array",
            "items": {
                "type": "object",
                "required": ["decision-id"],
                "properties": {
                    "decision-id": {
                        "type": "string",
                        "description": "Unique identifier — must match the decision JSON file name (without .json) and the trailing segment of the decision branch name"
                    }
                },
                "additionalProperties": false
            }
        }
    },
    "additionalProperties": false
}
