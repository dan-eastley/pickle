name: Decision Challenger

# Stub. Implementation pending.
# Chains after Decision Proponent via workflow_run.
# Reads the decision JSON, writes a stub to the `challenger` section,
# commits back via GITHUB_TOKEN.

on:
  workflow_run:
    workflows: ["Decision Proponent"]
    types: [completed]

permissions:
  contents: write

jobs:
  challenger:
    if: ${{ github.event.workflow_run.conclusion == 'success' && startsWith(github.event.workflow_run.head_branch, 'decisions/') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout decision branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_branch }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Write challenger stub to decision JSON
        env:
          BRANCH: ${{ github.event.workflow_run.head_branch }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WORKFLOW_NAME: "Decision Challenger"
          SECTION_KEY: "challenger"
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          bash .github/scripts/decision-stub.sh
