name: Validate Schema

on:
  push:

permissions:
  contents: read

jobs:
  validate-schema:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Started
        run: |
          echo "::notice::Started: Validate Schema"

      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Determine base ref to diff against
        id: base
        env:
          BRANCH: ${{ github.ref_name }}
        run: |
          case "$BRANCH" in
            main)        BASE="HEAD~1" ;;
            develop)     BASE="origin/main" ;;
            decisions/*) BASE="origin/main" ;;
            features/*)  BASE="origin/develop" ;;
            *)           BASE="origin/main" ;;
          esac

          # Fetch the remote ref so the diff has something to compare against.
          case "$BASE" in
            origin/*)
              REMOTE_BRANCH="${BASE#origin/}"
              git fetch origin "$REMOTE_BRANCH" 2>/dev/null || true
              ;;
          esac

          echo "base=$BASE" >> "$GITHUB_OUTPUT"
          echo "Comparing against: $BASE"

      - name: Find changed JSON files
        id: files
        env:
          BASE: ${{ steps.base.outputs.base }}
        run: |
          CHANGED=$(git diff --name-only --diff-filter=AM "$BASE" HEAD -- '*.json' || true)
          if [ -z "$CHANGED" ]; then
            echo "::notice::No JSON files changed since $BASE."
          else
            echo "Changed JSON files:"
            printf '  %s\n' $CHANGED
          fi
          {
            echo "files<<__EOF__"
            echo "$CHANGED"
            echo "__EOF__"
          } >> "$GITHUB_OUTPUT"

      - name: Install jsonschema
        if: steps.files.outputs.files != ''
        run: pip install jsonschema

      - name: Validate JSON syntax and (where applicable) against schema
        if: steps.files.outputs.files != ''
        env:
          FILES: ${{ steps.files.outputs.files }}
        run: |
          python3 <<'PYEOF'
          import json
          import os
          import pathlib
          import sys

          from jsonschema import Draft202012Validator

          files = [f for f in os.environ.get('FILES', '').strip().split('\n') if f]


          def schema_for(path):
              """Return the schema path for a data file, or None if no schema applies."""
              p = pathlib.Path(path)
              parts = p.parts

              # architectures/architectures.json -> config/schemas/architectures.json
              if path == 'architectures/architectures.json':
                  return pathlib.Path('config/schemas/architectures.json')

              # architectures/<arch>/architecture.json -> config/schemas/architecture.json
              if len(parts) == 3 and parts[0] == 'architectures' and parts[-1] == 'architecture.json':
                  return pathlib.Path('config/schemas/architecture.json')

              # architectures/<arch>/transitions.json -> config/schemas/transitions.json
              if len(parts) == 3 and parts[0] == 'architectures' and parts[-1] == 'transitions.json':
                  return pathlib.Path('config/schemas/transitions.json')

              # architectures/<arch>/<transition>/transition.json -> config/schemas/transition.json
              if len(parts) == 4 and parts[0] == 'architectures' and parts[-1] == 'transition.json':
                  return pathlib.Path('config/schemas/transition.json')

              # architectures/<arch>/<transition>/decisions/decisions.json -> config/schemas/decisions.json (index)
              if (
                  len(parts) == 5
                  and parts[0] == 'architectures'
                  and parts[3] == 'decisions'
                  and parts[4] == 'decisions.json'
              ):
                  return pathlib.Path('config/schemas/decisions.json')

              # architectures/<arch>/<transition>/decisions/<decision-id>/decision.json -> config/schemas/decision.json
              if (
                  len(parts) == 6
                  and parts[0] == 'architectures'
                  and parts[3] == 'decisions'
                  and parts[5] == 'decision.json'
              ):
                  return pathlib.Path('config/schemas/decision.json')

              # architectures/<arch>/<transition>/domains/<dom>/<layer>/<ID>.json
              #   -> config/schemas/artefacts/domains/<dom>/<layer>/<ID>.json
              if (
                  len(parts) == 7
                  and parts[0] == 'architectures'
                  and parts[3] == 'domains'
              ):
                  dom, layer, artefact_id = parts[4], parts[5], pathlib.Path(parts[6]).stem
                  return pathlib.Path(f'config/schemas/artefacts/domains/{dom}/{layer}/{artefact_id}.json')

              # Schema files themselves have no cross-reference target — JSON-parse only.
              return None


          failed = []

          for f in files:
              fp = pathlib.Path(f)
              if not fp.exists():
                  print(f"[skip] {f}: deleted")
                  continue

              # 1. JSON syntax check
              try:
                  with open(fp) as fh:
                      data = json.load(fh)
                  print(f"[json-ok] {f}")
              except Exception as e:
                  print(f"::error file={f}::Invalid JSON: {e}")
                  failed.append(f)
                  continue

              # 2. Cross-reference schema (if one applies)
              schema_path = schema_for(f)
              if schema_path is None:
                  print(f"[no-schema] {f}: JSON-parse only (no schema mapping)")
                  continue

              if not schema_path.exists():
                  print(f"::error file={f}::Expected schema at {schema_path} but it does not exist")
                  failed.append(f)
                  continue

              with open(schema_path) as fh:
                  schema = json.load(fh)

              validator = Draft202012Validator(schema)
              errors = sorted(validator.iter_errors(data), key=lambda e: list(e.absolute_path))
              if errors:
                  for err in errors:
                      loc = '/'.join(str(p) for p in err.absolute_path) or '(root)'
                      print(f"::error file={f}::{err.message} (at {loc})")
                  failed.append(f)
              else:
                  print(f"[schema-ok] {f} -> {schema_path}")

          if failed:
              print(f"\nValidation failed for {len(failed)} file(s):")
              for f in failed:
                  print(f"  - {f}")
              sys.exit(1)

          print("\nAll changed JSON files passed validation.")
          PYEOF

      - name: Finished
        if: always()
        run: |
          echo "::notice::Finished: Validate Schema"
